AdPlug/Winamp To Do List:
-------------------------
- Add tooltips to all configuration boxes [riven-mage]
- Make PortTalk support only occupy ports ADLIB_BASEPORT and ADLIB_BASEPORT + 1, instead of the whole range. [dynamite]
